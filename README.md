Converts Files from .gpx format to the required .txt format, allowing users to upload to a Masutec GPS using a serial interface. 

Currently Compatible/Tested with .GPX files generated from OpenCPN & Navionics
